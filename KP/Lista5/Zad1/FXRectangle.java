import javafx.geometry.Bounds;

import java.awt.Rectangle;

import javafx.event.ActionEvent;
import javafx.event.EventHandler;
import javafx.scene.input.MouseEvent;
import javafx.scene.input.ScrollEvent;
import javafx.scene.shape.Ellipse;

public class FXRectangle extends Rectangle {
    //Atrybuty
    private final double points[][] = new double[4][4]; //Wierzchołki prostokąta

    //Tworzenie prostokąta
    class FXRectangleMaking implements EventHandler<MouseEvent> {
        FXRectangle rectangle; 

    }

    private void getCorner(MouseEvent) { 
        
    }
}
